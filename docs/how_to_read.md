# How To Read This Project

Use `SCAFFOLD.md` as the project charter and the files under `scaffolds/` as
build prompts. Use `methods/`, `schemas/`, `config/`, `persons/`, `groups/`,
and `analyses/` as the active research workspace.

Every actor profile should be read as an interpretive hypothesis under
validation pressure, not as a completed psychological diagnosis.

